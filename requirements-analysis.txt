# Dependencies for dataset forensics + retrieval experiment (analysis/)
# retrieval module uses stdlib sqlite3 + csv only (no extra deps)
duckdb>=1.0.0
polars>=1.0.0
pyarrow>=15.0.0
rapidfuzz>=3.0.0
pandas>=2.0.0
